	<div class="span3 visible-desktop">
		<div class="feed-sidebar">
		<h2>Time Dollar Network</h2>
			<ul class="nav nav-list">
				<li class="nav-header">
					<a href="/users/sign_up">Account</a>
				</li>
				<li class="<% if params[:controller].to_sym == :users and controller.action_name.to_sym == :new %>active<% end %>">
					<a href="/users/sign_up">Sign Up</a>
				</li>
				
				<li class="<% if params[:controller] == "devise/sessions" and controller.action_name.to_sym == :new %>active<% end %>">
					<a href="/users/sign_in">Sign In</a>
				</li>
				
				<li class="divider"></li>
				
				<li class="nav-header">
					About the Network
				</li>
				<li class="<% if params[:controller].to_sym == :welcome and controller.action_name.to_sym == :about %>active<% end %>">
					<a href="/about#about">About Us</a>
				</li>
				<li class="<% if params[:controller].to_sym == :welcome and controller.action_name.to_sym == :about %>active<% end %>">
					<a href="/about">How the Network Works</a>
				</li>
				<li class="<% if params[:controller].to_sym == :welcome and controller.action_name.to_sym == :privacy %>active<% end %>">
					<a href="/privacy">Privacy Policy</a>
				</li>
			</ul>
		</div>
	</div>